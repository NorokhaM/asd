pipeline {
	agent {
		label 'docker-sping-boot'
	}
    environment {
		DOCKER_CREDENTIALS_ID = 'dockerhub'
		DOCKER_IMAGE = 'myapp:latest'
	}

	stages {
		stage('Build') {
			steps {
				script {

				}
			}
		}
	}
}